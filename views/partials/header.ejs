<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
  <nav class="w-full fixed top-0 left-0 px-6 py-4 bg-white shadow-md flex justify-between items-center z-50">
    <h3 class="text-2xl font-semibold text-gray-800">Scatch</h3>
    
    <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn===true) { %>
      <div class="flex gap-6 text-base font-medium text-gray-700">
        <a href="#" class="hover:text-blue-600 transition duration-200">Shop</a>
        <a href="#" class="hover:text-blue-600 transition duration-200">Cart</a>
        <a href="#" class="hover:text-blue-600 transition duration-200">My Account</a>
        <a href="http://localhost:8000/users/logout" class="text-red-500 hover:text-red-700 transition duration-200">Logout</a>
      </div>
    <% } %>
  </nav>
</body>
</html>
